# Description
```
The password for the next level is stored in a hidden file in the **inhere** directory.
```
---
Using `ls -alh` on `~` we see a directory `inhere`. Using `ls -lah` there, points us to a hidden filename: `...Hiding-From-You`. Reading it:
```shell
$ cat ./...Hiding-From-You
FLAG{}
```

# Flag 
```
2WmrDFRmJIq3IPxneAaMGhap0pFhF3NJ
```